import React from "react";
import { Styles } from "../Navbar/styles";
import { Link } from "react-router-dom";
const NavbarButtons = () => {
    const handleClick = () =>{
        
    }
  return (
    <Styles>
        <button>Change Banner Color</button>
        <button>Change Text Color</button>

    </Styles>
  );
};

export default NavbarButtons;
